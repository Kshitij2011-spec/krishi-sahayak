flask==3.1.1
flask-cors==5.0.1
scikit-learn==1.6.1
joblib==1.4.2
numpy<2.0.0
# pandas>=2.2.3
gunicorn==23.0.0
requests
google-genai
# ── Supabase admin client (service-role key, officer_requests) ────────────────
supabase
# ── Local MobileNetV2 inference ───────────────────────────────────────────────
# CPU-only torch wheels (~180 MB total) — avoids pulling 2.4 GB CUDA build
# which exceeds Render free-tier disk limits and crashes the build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu
transformers==4.40.2
Pillow

# Krishi-Sahayak Agent Rules

## Feature Status
- **Mandi Prices:** Live integration with data.gov.in Agmarknet API (not a stub).
- **TTS (Text-to-Speech):** Live using Web Speech API with a click-to-play button on the Crop Recommendation results page.

## Deployment Guidelines
- **Deploy via Git:** Every deployment must go through a git commit first. Never perform a direct "deploy from memory" bypass or manual extraction of files to deploy directly via CLI without committing. This ensures source of truth matches the live code.
